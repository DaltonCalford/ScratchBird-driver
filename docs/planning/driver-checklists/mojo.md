# Mojo Driver Checklist

Note: Integration priorities map to checklist buckets as follows: P0 → P1 (Core), P1 → P2 (Follow-ups), P2 → P3 (Future).

## P0 (Blocking)

- [ ] Replace Python bridge with native SBWP client in `tracks/p3/drivers/mojo/src/scratchbird.mojo`. Progress: `src/scratchbird.mojo` now compiles in current Mojo syntax as a facade over `src/scratchbird_native.mojo`, with deterministic module-surface smoke in `tests/scratchbird_surface.mojo`; native bootstrap coverage in `tests/native_bootstrap.mojo` now includes connect/ping guards, metadata alias/query resolution plus executable metadata rowcount helpers, restriction-aware metadata query shaping helpers (`normalize_metadata_restriction_key`, `resolve_metadata_collection_query_restricted`, `resolve_metadata_collection_query_restricted_multi`, `query_metadata_restricted`, `query_metadata_rows_restricted`, `query_metadata_restricted_multi`, `query_metadata_rows_restricted_multi`) with expanded alias keys (`catalog`/`index`/`constraint`/`routine`/`type`) and schema/table/index/constraint/routine/type restriction predicates across table/column/index/constraint/key/privilege/routine families, plus wildcard (`LIKE ... ESCAPE '\\'`) and null (`IS NULL`) restriction shaping (including escaped wildcard patterns) and SQLSTATE guards (`07001`/`22023`), deterministic DDL-editor payload shaping helpers (`build_ddl_editor_schema_payload`, `ddl_editor_schema_payload`), transaction lifecycle guard/no-op semantics, savepoint lifecycle guards (`25000`/`3B001`), prepare-bind mismatch handling, prepared execute parity, paging-query rowcount semantics, stream/cancel with post-cancel recovery smoke, deterministic SQLSTATE-prefixed native guard/unsupported error strings, active lifecycle hooks for circuit-breaker/keepalive/telemetry plus leak-detector/pipeline scaffolds, deterministic DSN knobs for lifecycle tuning (`cb_*`, `keepalive_*`, `leak_*`, `pipeline_*`), native SQLSTATE guards (`54000` capacity / `08006` breaker-open), auto/manual pipeline flush behavior, and breaker half-open recovery assertions. Integration/conformance launchers (`tracks/p3/drivers/mojo/tests/integration.py`, `tracks/p3/drivers/mojo/tests/sbdriver_conformance.py`) now run both `scratchbird_surface.mojo` and `native_bootstrap.mojo` before shim smoke; integration smoke validates transaction/savepoint lifecycle and prepare/stream-cancel recovery checks, metadata stability invariants across schemas/tables/columns, deterministic fallback content, and payload-shaping checks; integration/conformance now support direct/manager/listener DSN matrices (`SCRATCHBIRD_MOJO_*_URLS`) with opt-in wire bridge routing (`sb_wire_transport=python`) and CI live-matrix gating. Full pure-Mojo socket/TLS transport cutover remains open.
- [x] Ongoing progress notes (2026-03-06): native bootstrap now parses DSN credential/endpoint fields (`user`/`password`, `host`, `port`) with query override and alias support (`user|username|pguser`, `password|passwd|pgpassword`, `host|hostname|servername|pghost`, `port|portNumber|pgport`, `database|dbname|databaseName|pgdatabase`) plus bracketed IPv6 host parsing, parses JDBC-style session knobs (`role`, `application_name|applicationname`, `autocommit|auto_commit`, `readonly|read_only`, `current_schema|search_path|searchPath|currentschema`, `default_row_fetch_size|fetch_size|fetchSize|defaultrowfetchsize`, `metadata_expand_schema_parents|metadataexpandschemaparents|expand_schema_parents|expandschemaparents|dbeaver_expand_schema_parents|dbeaverexpandschemaparents`), parses JDBC parity knobs for statement/logging behavior (`prepare_threshold|preparethreshold`, `rewrite_batched_inserts|rewritebatchedinserts`, `logger_level|loggerlevel|log_level|loglevel`, `logger_file|loggerfile|log_file|logfile`), parses TLS material knobs (`sslrootcert`, `sslcert`, `sslkey`, `sslpassword`, plus underscore aliases), parses pooling/manager knobs (`tcpkeepalive`, `pooling`, `min_pool_size|minpoolsize`, `max_pool_size|maxpoolsize`, `connection_lifetime|connectionlifetime|poolingconnectionlifetime`, `manager_*|mcp_*` with defaults `manager_connection_profile=native_v3`, `manager_client_intent=native_v3`, `manager_auth_fast_path=true`), normalizes front-door mode aliases (`manager-proxy`/`managerproxy`/`managed`) with query-order precedence (last matching key across `front_door_mode|frontdoormode|connection_mode|ingress_mode` wins) and `0A000` invalid-mode guards, parses timeout aliases (`connect_timeout|connecttimeout`, `socket_timeout|sockettimeout`, `login_timeout|logintimeout`, `acquire_timeout|acquiretimeout`, fallback `pooling_acquire_timeout|poolingacquiretimeout`) with endpoint/timeout guard assertions (host defaults to `localhost` when omitted; explicit empty host rejected; `port` in `1..65535`; timeout values `>= 0`, `default_row_fetch_size >= 0`, `min_pool_size >= 0`, `max_pool_size >= 1`, `connection_lifetime >= 0`, `manager_client_flags >= 0`), canonicalizes protocol aliases (`scratchbird`, `scratchbird-native`, `scratchbird_native` → `native`), parses `ssl` as an alias for `sslmode` (including accepted `disable`), accepts `binary_transfer|binarytransfer` with JDBC-compatible `false` behavior, normalizes compression values (`none` → `off`) and accepts JDBC-compatible `zstd` while rejecting unsupported algorithms (for example `gzip`), decodes URL-style DSN query values (`%xx`, `+`) with malformed-escape guard (`22023`), rejects malformed bracketed-IPv6 authority hosts and malformed integer DSN overrides with deterministic `22023` guards, enforces native protocol selection (`protocol|parser|dialect` must resolve to `native`), requires manager token in manager-proxy mode (`08001`), and uses deterministic endpoint-aware connection identity (`user@host:port/database`) in lifecycle hooks; native/facade connect guards include deterministic auth-fail parity (`sb_test_auth_fail=true` → `28P01`) with smoke assertions; bridge runtime now supports opt-in wire transport routing (`sb_wire_transport=python` / `SCRATCHBIRD_MOJO_WIRE_TRANSPORT`) through `_PythonWireConnection`, integration/conformance runtime matrices now cover direct/manager/listener DSN sets (`SCRATCHBIRD_MOJO_*_URLS`), and Mojo CI gate now includes `tests/wire_transport_bridge.py` with optional live matrix execution (`MOJO_LIVE_MATRIX_ENABLED`).
- [x] Enforce TLS required and binary-only guard rails in current bridge shim (`tracks/p3/drivers/mojo/src/scratchbird.py`) with deterministic tests in `tracks/p3/drivers/mojo/tests/connection_guards.py`. Issue: DONE (2026-03-05)
- [x] Maintain legacy bridge-shim rejection for `compression=zstd` (`tracks/p3/drivers/mojo/src/scratchbird.py`) with deterministic tests in `tracks/p3/drivers/mojo/tests/connection_guards.py`. Issue: DONE (2026-03-05)

## P1 (Core)

- [x] Implement bridge-shim type encode/decode wrappers in `tracks/p3/drivers/mojo/src/scratchbird.py` with deterministic tests in `tracks/p3/drivers/mojo/tests/type_codecs.py`. Issue: DONE (2026-03-05)
- [x] Expand bridge-shim type support to composite/geometry/inet-cidr-macaddr (`tracks/p3/drivers/mojo/src/scratchbird.py`) with deterministic coverage in `tracks/p3/drivers/mojo/tests/type_codecs.py`. Issue: DONE (2026-03-05)
- [x] Add sys.* metadata helpers in bridge lane (`tracks/p3/drivers/mojo/src/scratchbird.py`) with alias/query coverage in `tracks/p3/drivers/mojo/tests/metadata_execution.py`. Issue: DONE (2026-03-05)

## P2 (Follow-ups)

- [x] Add conformance/integration coverage for bridge lane (`tracks/p3/drivers/mojo/tests/sbdriver_conformance.py`, `tracks/p3/drivers/mojo/tests/integration.py`, `tracks/p3/drivers/mojo/tests/connection_guards.py`). Issue: DONE (2026-03-05)

## P3 (Future)
